@PACKAGE_INIT@

set_and_check(echocv_INCLUDE_DIRS "@PACKAGE_INCLUDE_INSTALL_DIR@")
set_and_check(echocv_LIBRARY_DIRS "@PACKAGE_LIB_INSTALL_DIR@")

include("${CMAKE_CURRENT_LIST_DIR}/echocv-targets.cmake")

set(echocv_LIBRARIES echocv)
set(echocv_FOUND TRUE)
